diff --git a/include/class_loader/class_loader_core.hpp b/include/class_loader/class_loader_core.hpp
index f41a87f..160fa9f 100644
--- a/include/class_loader/class_loader_core.hpp
+++ b/include/class_loader/class_loader_core.hpp
@@ -355,6 +355,11 @@ Base * createInstance(const std::string & derived_class_name, ClassLoader * load
   FactoryMap & factoryMap = getFactoryMapForBaseClass<Base>();
   if (factoryMap.find(derived_class_name) != factoryMap.end()) {
     factory = dynamic_cast<impl::AbstractMetaObject<Base> *>(factoryMap[derived_class_name]);
+#if defined(__QNX_)
+    if (factory == nullptr) {
+      factory = static_cast<impl::AbstractMetaObject<Base> *>(factoryMap[derived_class_name]);
+    }
+#endif // __QNX__
   } else {
     CONSOLE_BRIDGE_logError(
       "class_loader.impl: No metaobject exists for class type %s.", derived_class_name.c_str());
@@ -381,6 +386,10 @@ Base * createInstance(const std::string & derived_class_name, ClassLoader * load
         "possible to shutdown the library!");
 
       obj = factory->create();
+#if defined(__QNX__)
+    } else if (factory) {
+      obj = factory->create();
+#endif // __QNX__
     } else {
       throw class_loader::CreateClassException(
               "Could not create instance of type " + derived_class_name);
@@ -416,6 +425,10 @@ std::vector<std::string> getAvailableClasses(const ClassLoader * loader)
       classes.push_back(it.first);
     } else if (factory->isOwnedBy(nullptr)) {
       classes_with_no_owner.push_back(it.first);
+#if defined(__QNX__)
+    } else {
+      classes_with_no_owner.push_back(it.first);
+#endif // __QNX__
     }
   }
 
